(tests
  (names arith let_bind simple_func conditional factorial global_names tuple definition variant value_match or_pattern when_match anon_function closure_currying comment equality let_and list string record range_pattern)
  (libraries mlml unix))
